# Система модерации объявлений

Веб-приложение для модерации объявлений на платформе Авито. Реализовано в рамках тестового задания для стажёра Frontend.

## 🚀 Технологии

**Frontend:**

- React 18+ с TypeScript
- Chakra-UI для компонентов интерфейса
- React Router для навигации
- Redux для управления состоянием
- Vite для сборки

**Backend:**

- Node.js + Express.js
- RESTful API

## ✨ Реализованный функционал

### 📋 Список объявлений (`/list`)

- Отображение объявлений в виде карточек с изображениями, ценой, статусом
- **Фильтрация:** по статусу (множественный выбор), категории, диапазону цен
- **Поиск:** по названию объявления с debounce
- **Сортировка:** по дате, цене, приоритету (возрастание/убывание)
- **Пагинация:** 10 объявлений на страницу с навигацией

### 📄 Детальная страница объявления (`/item/:id`)

- Полная информация об объявлении: галерея изображений, описание, характеристики
- Информация о продавце: имя, рейтинг, количество объявлений, дата регистрации
- История модерации с действиями, модераторами и комментариями
- **Действия модератора:**
  - Одобрить объявление
  - Отклонить (с обязательным указанием причины)
  - Вернуть на доработку (с обязательным указанием причины)
- Навигация: предыдущее/следующее объявление

### 📊 Статистика модератора (`/stats`)

- Метрики: всего проверено, проценты одобренных/отклоненных, среднее время проверки
- График активности по дням (столбчатая диаграмма)
- Круговая диаграмма распределения решений
- График по категориям проверенных объявлений
- Фильтр по периоду: сегодня, 7 дней, 30 дней

## 🏗️ Архитектура

```
client/
├── src/
│   ├── api/          # API функции с типизацией
│   ├── components/   # Переиспользуемые компоненты
│   ├── container/    # Основные компоненты
│   ├── redux/        # Страницы приложения
│   ├── types/        # TypeScript типы
```

## 🎯 Особенности реализации

- **Полная типизация TypeScript** — типобезопасность на всех уровнях
- **UX улучшения:**
  - Единые компоненты загрузки и ошибок
  - Валидация форм
  - Автоматическое обновление данных после действий
- **Чистый код:**
  - Разделение ответственности
  - Переиспользуемые компоненты
  - Единый стиль кода

## 🛠️ Запуск проекта

### Предварительные требования

- Node.js v20+
- npm или yarn

### Запуск сервера

```bash
cd tech-int3-server
npm install
npm start
```

Сервер будет доступен на `http://localhost:3001`

### Запуск клиента

```bash
cd system-moderation
npm install
npm run dev
```

Клиент будет доступен на `http://localhost:5173` (или другом порту, указанном Vite)

## 📝 API Endpoints

- `GET /api/v1/list` — получить список объявлений
- `GET /api/v1/ads/:id` — получить объявление по ID
- `POST /api/v1/ads/:id/approve` — одобрить объявление
- `POST /api/v1/ads/:id/reject` — отклонить объявление
- `POST /api/v1/ads/:id/request-changes` — вернуть на доработку
- `GET /api/v1/stats/summary` — общая статистика
- `GET /api/v1/stats/chart/activity` — график активности
- `GET /api/v1/stats/chart/decisions` — график решений
- `GET /api/v1/stats/chart/categories` — график по категориям

## ✅ Соответствие заданию

Все обязательные требования выполнены:

- ✅ React v18+ с TypeScript
- ✅ react-router-dom для роутинга
- ✅ Использование готового API
- ✅ Все функциональные требования реализованы
- ✅ Фильтрация, сортировка, пагинация
- ✅ Действия модератора с валидацией
- ✅ Статистика с графиками

## 📦 Сборка для продакшена

```bash
cd system-moderation
npm run build
```
